@model Common.Credit

@{
    ViewData["Title"] = "Add";
}

<h1>Add</h1>

<h4>Credit</h4>
<hr />





<div class="row">
    <div class="col-md-4">
        <form method="post">
            <table>
                <tr>
                    <td>10x Credit:</td>
                    <td><input type="number" value="10" name="credit10" /></td>
                </tr>
                <tr>
                    <td>10x Credit:</td>
                    <td><input type="number" value="15" name="credit20" /></td>
                </tr>
                <tr>
                    <td>10x Credit:</td>
                    <td><input type="number" value="20" name="credit30" /></td>
                </tr>
            </table>
            <div>
                <input type="submit" value="Append" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
