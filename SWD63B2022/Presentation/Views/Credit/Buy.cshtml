@model IEnumerable<Common.Credit>

@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.credit10)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.credit20)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.credit30)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.credit10)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.credit20)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.credit30)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
               
            </td>
        </tr>
}
    </tbody>
</table>
